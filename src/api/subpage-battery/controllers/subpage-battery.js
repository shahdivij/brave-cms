'use strict';

/**
 * subpage-battery controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::subpage-battery.subpage-battery');
