'use strict';

/**
 * subpage-processor service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::subpage-processor.subpage-processor');
